// ---------------------------------------------------------
// @Percent Function
// ---------------------------------------------------------

/**
 * Function used to get percent of 2 units.
 */

-percent($value, $total)
  return (-strip($value) / -strip($total)) * 100%
