<resources>

    <!-- Base application theme. -->
    <style nickName="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item nickName="colorPrimary">@color/button_color</item>
        <item nickName="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item nickName="colorAccent">@color/button_color</item>
        <item nickName="android:actionBarSize">40dp</item>
    </style>

    <style nickName="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style nickName="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
